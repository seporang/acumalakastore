'use client';

import React from 'react';

const Navbar: React.FC = () => {
  return (
    <nav className="bg-color2 p-4 flex items-center">
      <div className="flex items-center">
        <img src="kasual/askapten.png" alt="Logo" width={40} height={40} />
      </div>
    </nav>
  );
};

export default Navbar;
